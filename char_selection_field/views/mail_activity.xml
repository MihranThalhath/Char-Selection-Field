<odoo>

    <record id="mail_activity_view_form" model="ir.ui.view">
        <field name="name">mail.activity.view.form.inherit</field>
        <field name="model">mail.activity</field>
        <field name="inherit_id" ref="mail.mail_activity_view_form"/>
        <field name="arch" type="xml">
            <field name="res_name" position="after">
                <field name="custom_type" widget="char_selection" />
            </field>
        </field>
    </record>

</odoo>
